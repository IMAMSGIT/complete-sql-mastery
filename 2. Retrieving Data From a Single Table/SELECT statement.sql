USE sql_store;
SELECT * # SELECT only selects columns
FROM customers # The table name
# WHERE customer_id=1 # condition on any columns
ORDER BY first_name
### SELECT is the must clause, others are not
### And their orders matter
