-- SELECT last_name,first_name,points,points+10 as "DISCOUNT OFFER" # use quote to get space in the name
-- FROM customers
-- SELECT DISTINCT state # DISTINCT coolects only unique types
-- FROM customers

-- Exercise
SELECT name,unit_price,unit_price*1.1 AS "new price"
FROM products